apiVersion: v1
kind: Service
metadata:
  name: kiro-app-service
  namespace: kiro-app
  labels:
    app: kiro-app
spec:
  type: LoadBalancer
  selector:
    app: kiro-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    name: http
  sessionAffinity: None
